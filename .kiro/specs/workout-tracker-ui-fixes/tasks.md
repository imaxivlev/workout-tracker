# План реализации: Исправления UI для Workout Tracker

## Обзор

Этот план описывает пошаговую реализацию исправлений UI для приложения CrossFit Workout Tracker. Все изменения будут внесены в существующие файлы: `index.html`, `mvp-script-v3.js` и `mvp-styles.css`.

## Задачи

- [x] 1. Исправить поведение полей ввода дат в календаре
  - [x] 1.1 Реализовать функцию маски ввода для полей дат
    - Создать функцию `setupDateInputMask(inputElement)` в `mvp-script-v3.js`
    - Добавить обработчик `onclick` для выделения всего текста
    - Добавить обработчик `oninput` для форматирования ввода как ДД.ММ.ГГГГ
    - Добавить обработчик `onkeydown` для защиты разделителей от удаления
    - _Requirements: 1.1, 1.2, 1.3, 1.4_
  
  - [x] 1.2 Применить маску к полям ввода дат
    - Вызвать `setupDateInputMask()` для `#start-date-input` и `#end-date-input` при инициализации календаря
    - Обновить функции `focusStartDate()` и `focusEndDate()` для работы с маской
    - _Requirements: 1.1, 1.2, 1.3, 1.4_
  
  - [ ]* 1.3 Написать property test для маски ввода даты
    - **Property 1: Маска ввода даты**
    - **Validates: Requirements 1.2, 1.3, 1.4**
  
  - [ ]* 1.4 Написать unit test для выделения текста при клике
    - **Property 2: Выделение текста при клике**
    - **Validates: Requirements 1.1**

- [x] 2. Реализовать гибкость выбора диапазона дат
  - [x] 2.1 Удалить ограничение на выбор даты "До" без даты "От"
    - Удалить проверку `if (!datePickerState.startDate)` и alert из функции `focusEndDate()`
    - _Requirements: 2.1_
  
  - [x] 2.2 Реализовать автоматическую сортировку дат
    - Обновить функцию `selectDate()` для автоматической сортировки дат
    - Если выбранная дата меньше startDate, поменять их местами
    - _Requirements: 2.2, 2.3_
  
  - [ ]* 2.3 Написать unit test для выбора даты "До" без даты "От"
    - **Example 1: Выбор даты "До" без даты "От"**
    - **Validates: Requirements 2.1**
  
  - [ ]* 2.4 Написать property test для автоматической сортировки дат
    - **Property 3: Автоматическая сортировка дат**
    - **Validates: Requirements 2.2, 2.3**

- [x] 3. Ограничить доступные месяцы в календаре
  - [x] 3.1 Усилить блокировку навигации к будущим месяцам
    - Обновить функцию `changeMonth(delta)` для предотвращения навигации к будущим месяцам
    - Добавить проверку что последний отображаемый месяц не превышает текущий месяц
    - _Requirements: 3.1, 3.2_
  
  - [x] 3.2 Обновить функцию updateNavButtons
    - Убедиться что кнопка "вперед" корректно скрывается когда достигнут текущий месяц
    - _Requirements: 3.1, 3.2_
  
  - [x] 3.3 Исправить навигацию назад в календаре
    - Проверить и исправить обработчик события для кнопки навигации назад
    - Убедиться что `changeMonth(-1)` корректно работает
    - Добавить функцию `setupNavigationButtons()` если её нет
    - _Requirements: 9.1_
  
  - [x] 3.4 Изменить отображение месяцев по умолчанию
    - Обновить `getInitialDisplayMonth()` чтобы показывать текущий месяц как последний (справа)
    - По умолчанию показывать текущий и 2 предыдущих месяца (не следующие)
    - _Requirements: 9.2, 9.3_
  
  - [ ]* 3.5 Написать unit test для текущего месяца как последнего
    - **Example 2: Текущий месяц как последний**
    - **Validates: Requirements 3.1**
  
  - [ ]* 3.6 Написать property test для блокировки будущих месяцев
    - **Property 4: Блокировка будущих месяцев**
    - **Validates: Requirements 3.2**
  
  - [ ]* 3.7 Написать property test для разрешения прошлых месяцев
    - **Property 5: Разрешение прошлых месяцев**
    - **Validates: Requirements 3.3**

- [x] 4. Реализовать адаптивное отображение календаря
  - [x] 4.1 Создать функцию определения количества месяцев
    - Создать функцию `getMonthsToDisplay()` которая возвращает 1, 2 или 3 в зависимости от ширины viewport
    - < 481px: 2 месяца (изменено с 1)
    - 481-768px: 2 месяца
    - > 768px: 3 месяца
    - _Requirements: 4.1, 4.2, 4.3, 4.4, 10.1, 10.2, 10.3_
  
  - [x] 4.2 Создать функцию определения начального месяца
    - Создать функцию `getInitialDisplayMonth()` которая определяет первый отображаемый месяц
    - Если даты выбраны: показать последний выбранный месяц как последний в ряду
    - Если даты не выбраны: показать текущий месяц как последний в ряду
    - _Requirements: 4.1, 4.2, 4.3, 4.4, 10.2_
  
  - [x] 4.3 Обновить функцию renderCalendar
    - Использовать `getMonthsToDisplay()` и `getInitialDisplayMonth()`
    - Динамически рендерить нужное количество месяцев
    - _Requirements: 4.1, 4.2, 4.3, 4.4_
  
  - [x] 4.4 Добавить обработчик изменения размера окна
    - Добавить debounced event listener на `window.resize`
    - При изменении размера перерисовывать календарь если он открыт
    - _Requirements: 4.1, 4.2, 4.3, 4.4_
  
  - [x] 4.5 Исправить CSS для адаптивной ширины календаря
    - Добавить media query для планшетов (481-768px): grid-template-columns: repeat(2, 1fr)
    - Добавить media query для мобильных (<481px): grid-template-columns: repeat(2, 1fr)
    - Добавить центрирование для мобильных устройств
    - Убедиться что месяцы занимают всю ширину контейнера с учетом отступов
    - _Requirements: 10.1, 10.3, 10.4, 10.5_
  
  - [ ]* 4.6 Написать unit tests для адаптивного отображения
    - **Example 3-7: Адаптивное отображение для разных размеров экрана**
    - **Validates: Requirements 4.1, 4.2, 4.3, 4.4, 10.1, 10.2, 10.3**

- [ ] 5. Checkpoint - Проверить работу календаря
  - Убедиться что все изменения календаря работают корректно
  - Протестировать на разных размерах экрана
  - Спросить пользователя если возникли вопросы

- [x] 6. Унифицировать стили выпадающих списков
  - [x] 6.1 Обновить базовые стили .form-select
    - Открыть `mvp-styles.css`
    - Обновить стили `.form-select` для добавления всех необходимых свойств
    - Добавить стили для `:hover`, `:focus`, `:disabled`
    - Добавить кастомную стрелку через `background-image`
    - Убедиться что `appearance: none` применен
    - _Requirements: 5.1, 5.2, 5.3, 5.4, 5.5_
  
  - [x] 6.2 Удалить специфичные переопределения стилей
    - Найти и удалить все специфичные стили для отдельных выпадающих списков
    - Убедиться что все выпадающие списки используют только базовый класс `.form-select`
    - _Requirements: 5.1, 5.2, 5.3, 5.4, 5.5_
  
  - [x] 6.3 Проверить применение класса во всех местах
    - История: фильтр по упражнению
    - Создать тренировку SKILL: количество подходов
    - Создать тренировку WOD: тип, уровень, количество раундов, результат
    - Статистика: выбор упражнения, уровень детализации
    - _Requirements: 5.1, 5.2, 5.3, 5.4, 5.5_
  
  - [ ]* 6.4 Написать property test для согласованности стилей
    - **Property 6: Согласованность стилей выпадающих списков**
    - **Validates: Requirements 5.1, 5.2, 5.3, 5.4**
  
  - [ ]* 6.5 Написать unit tests для конкретных выпадающих списков
    - **Example 8: Сохранение базовых стилей**
    - **Validates: Requirements 5.5**

- [x] 7. Исправить адаптивную верстку WOD для планшетов (481-768px)
  - [x] 7.1 Создать media query для планшетов
    - Добавить `@media (min-width: 481px) and (max-width: 768px)` в `mvp-styles.css`
    - _Requirements: 6.1, 6.2, 6.3, 6.4_
  
  - [x] 7.2 Реализовать layout для WOD без лесенки на планшетах
    - Использовать CSS Grid с двумя строками
    - Первая строка: Упражнение (полная ширина)
    - Вторая строка: Повт. и Вес (равной ширины, 1fr 1fr)
    - _Requirements: 6.1, 6.2_
  
  - [x] 7.3 Реализовать layout для WOD с лесенкой на планшетах
    - Использовать CSS Grid с тремя строками
    - Первая строка: Упражнение (полная ширина)
    - Вторая строка: Раунды (с горизонтальной прокруткой)
    - Третья строка: Вес (полная ширина)
    - _Requirements: 6.3, 6.4_
  
  - [ ]* 7.4 Написать unit tests для планшетного layout
    - **Example 9-12: WOD Tablet layout**
    - **Validates: Requirements 6.1, 6.2, 6.3, 6.4**

- [x] 8. Исправить адаптивную верстку WOD для мобильных (<481px)
  - [x] 8.1 Обновить media query для мобильных
    - Обновить существующий `@media (max-width: 480px)` в `mvp-styles.css`
    - _Requirements: 7.1, 7.2, 7.3, 7.4, 7.5, 12.1, 12.2_
  
  - [x] 8.2 Реализовать layout для WOD без лесенки на мобильных
    - Использовать CSS Grid
    - Первая строка: Упражнение (полная ширина)
    - Вторая строка: Повт. и Вес (равной ширины, 1fr 1fr)
    - _Requirements: 7.1_
  
  - [x] 8.3 Реализовать layout для WOD с лесенкой на мобильных
    - Использовать CSS Grid с вертикальным стеком
    - Первая строка: Упражнение (зафиксировано)
    - Вторая строка: Раунды (с горизонтальной прокруткой, overflow-x: auto)
    - Третья строка: Вес (зафиксировано)
    - Скрыть `.single-reps-container` в режиме лесенки
    - _Requirements: 7.2, 7.3, 7.4, 7.5_
  
  - [x] 8.4 Исправить отступы в режиме лесенки на мобильных
    - Установить grid-template-columns: 250px 1fr 100px 40px для .wod-exercise-row.ladder-mode
    - Убедиться что отступы от края полей до красного крестика оптимальны
    - _Requirements: 12.1, 12.2_
  
  - [ ]* 8.5 Написать unit tests для мобильного layout
    - **Example 13-17: WOD Mobile layout**
    - **Validates: Requirements 7.1, 7.2, 7.3, 7.4, 7.5, 12.1, 12.2**

- [x] 9. Исправить отображение пагинации
  - [x] 9.1 Обновить стили .pagination-container
    - Открыть `mvp-styles.css`
    - Убедиться что `flex-direction: row` установлен
    - Добавить `flex-wrap: wrap` для переноса на маленьких экранах
    - Убедиться что `justify-content: center` и `align-items: center` установлены
    - _Requirements: 8.1, 8.2_
  
  - [x] 9.2 Проверить стили .pagination-btn
    - Убедиться что кнопки используют `display: inline-flex`
    - Проверить что нет конфликтующих стилей
    - _Requirements: 8.1, 8.2_
  
  - [ ]* 9.3 Написать unit tests для пагинации
    - **Example: Горизонтальная пагинация**
    - **Property 7: Горизонтальная пагинация**
    - **Validates: Requirements 8.1, 8.2**

- [ ] 10. Стилизовать горизонтальную прокрутку на десктопе
  - [x] 10.1 Добавить стили scrollbar для .ladder-reps-container
    - Открыть `mvp-styles.css`
    - Добавить стили для scrollbar-width и scrollbar-color (Firefox)
    - Добавить стили для ::-webkit-scrollbar (Chrome, Safari, Edge)
    - Использовать CSS-переменные для цветов (--color-primary, --bg-tertiary)
    - _Requirements: 11.1, 11.2, 11.3_
  
  - [x] 10.2 Проверить согласованность стилей прокрутки
    - Убедиться что стили прокрутки на десктопе совпадают с адаптивной версией
    - Проверить что прокрутка появляется при количестве раундов > 6
    - _Requirements: 11.2, 11.3_

- [ ] 11. Checkpoint - Финальная проверка
  - Убедиться что все тесты проходят
  - Протестировать все изменения на разных размерах экрана (desktop, tablet, mobile)
  - Проверить что не появились регрессии в других частях приложения
  - Спросить пользователя если возникли вопросы

- [ ] 12. Настроить тестовое окружение (опционально)
  - [ ] 12.1 Установить зависимости для тестирования
    - Установить Jest, @testing-library/dom, jsdom
    - Установить fast-check для property-based testing
    - Создать конфигурацию Jest
    - _Requirements: Все_
  
  - [ ] 12.2 Создать helper функции для тестирования
    - Создать `setViewportWidth(width)` для симуляции размеров экрана
    - Создать `compareStyles(element1, element2, properties)` для сравнения стилей
    - Создать setup/teardown функции для тестов
    - _Requirements: Все_

## Примечания

- Задачи помеченные `*` являются опциональными и могут быть пропущены для быстрого MVP
- Каждая задача ссылается на конкретные требования для отслеживаемости
- Checkpoints обеспечивают инкрементальную валидацию
- Property tests валидируют универсальные свойства корректности
- Unit tests валидируют конкретные примеры и edge cases
- Все изменения должны быть обратно совместимы с существующим функционалом

